# @logue/web

logue のフロントエンド（React + Vite、Cloudflare Pages にデプロイ）。

- `npm run dev --workspace apps/web` で開発サーバー起動
- `npm run build --workspace apps/web` で本番ビルド
- lint / format / test はリポジトリルートから一括で実行する
